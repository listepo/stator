name: Stator Node and pnpm setup
description: Install the repository-pinned pnpm and Node toolchain
runs:
  using: composite
  steps:
    - uses: pnpm/action-setup@v4
    - uses: actions/setup-node@v4
      with:
        node-version-file: .node-version
        cache: pnpm
