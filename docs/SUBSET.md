# SUBSET.md — Feature support matrix for Stator

## Overview

This document specifies which language features Stator compiles in each mode, and how. The four verdict values are:

- **static** — compiled to unboxed machine values (i32, f64, struct fields, function pointers); the default fast path.
- **dynamic** — compiled via tagged NaN-boxed values with shape tables and inline caches; used for untyped code and type-narrowed unions in `js` mode.
- **error(CODE)** — compile error, by design, with a permanent diagnostic code. Features marked as errors will never be supported in that mode.
- **not-yet(CODE, phase)** — planned feature; the diagnostic code and phase name indicate when support arrives.

Each row becomes ≥1 decision test (§4 Task 1.4 in plan.md) per mode, executable via `stator explain --json`.

This matrix operationalizes plan.md §1 (product spec). Rows must not contradict the starting matrix in plan.md §4 Task 1.1. Rows marked **[proposed]** represent language features not listed there but that implementers will encounter early (e.g., `for`-`of`, template literals, destructuring, exceptions); each is documented in the "Codes allocated by this document" section with a defensible verdict consistent with the plan's philosophy.

### Mode semantics

`ts` mode rejects untyped code entirely; `js` mode accepts both `.ts` and `.js` files, treating untyped code as `Unknown` and lowering to the dynamic path (shape tables, inline caches, tagged values). One pipeline, two policy layers: file acceptance, diagnostic table, and typing of unresolved constructs.

---

## Syntax & control flow

| Feature | `ts` mode | `js` mode | Notes |
|---|---|---|---|
| `let`, `const` bindings | static | static if typed, else dynamic | Binding semantics are static; initialization and narrowing drive the static/dynamic split. |
| Function declarations, function expressions, arrow functions | static | static if typed, else dynamic | Closure capture via environment structs; untyped parameters widen to `Unknown`. Capturing a binding declared *inside a loop* is `STA1214` until loops carry per-iteration environments — one environment per call would give every iteration's closure the same slot. |
| `if` statements | static | static | Condition is evaluated; predicate narrowing is type-driven. |
| `while`, `do`/`while` loops | static | static | Control flow only. |
| `for` loop (C-style) **[proposed]** | static | static | Loop variable and bounds are typed; control flow is unboxed. |
| `for`...`of` loop over an array, a string, a Map, a Set, or a user class with `[Symbol.iterator]()` | static if the yielded type is typed, else dynamic | same | Specialized loops (docs/VALUE.md §4.13), no iterator object. Array length is re-read each step so a body that shortens the array stops early; strings are immutable so the bound is `jsrt_string_length`. A string yields **code points**, not UTF-16 units (`"👍"` is one iteration). A Set yields the element. A Map yields a `[key, value]` pair, which the HIR has no tuple for, so that binding is Unknown and a file that uses it is dynamic even when K and V are typed. Labels work on it like any other loop. A user class whose `[Symbol.iterator]()` returns a Generator (or a boxed specialized iterator) calls that method and drives the result with the existing iterator walk. `Symbol.iterator` as a stored value stays `not-yet(STA1212)`. |
| `for`...`in` loop **[proposed]** | dynamic | dynamic | Property enumerability is a runtime decision in both modes; no static optimization for for-in exists. |
| `switch` statement **[proposed]** | static | static if cases are typed, else dynamic | Control flow; case evaluation is type-driven. |
| `break` and `continue` **[proposed]** | static | static | Unlabeled and labeled control-flow redirection. |
| Labeled statements and labeled `break`/`continue` **[proposed]** | static | static | Compile to plain `goto` — not *computed* `goto`, which is a GCC extension taking a label's address and is not needed: every jump target is known at compile time. Unlabeled jumps use `goto` too, so a `break` inside a `switch` inside a loop cannot be captured by C's own binding rules. |
| Template literals **[proposed]** | static | static if interpolations are typed, else dynamic | String concatenation of typed values compiles to direct C concatenation; untyped interpolations use `jsrt_string_coerce()`. |
| Spread operator `...` in array literals **[proposed]** | static | static if spread source is typed, else dynamic | Untyped spread arrays route via `Unknown` shape dispatch. |
| Rest parameters `...args` | static | static if all consumers are typed, else dynamic | **Implemented (Phase 5 step 12 family a).** Callers already pass extra arguments; the callee packs `argv[i..]` into an array (`jsrt_args_rest`). `Function.length` excludes the rest parameter. Destructuring rest (`...[a, b]`) stays not-yet. |
| Default parameter values | static | static if typed, else dynamic | **Implemented (Phase 5 step 12 family a).** Evaluated when the argument is `undefined` (missing or explicit). `Function.length` stops at the first default. |
| Optional parameters `x?` | static | static if typed, else dynamic | **Implemented (Phase 5 step 12 family a).** A missing argument is `undefined`; no initializer runs. `Function.length` still counts the parameter. |
| Declaration without initializer `let x;` | static | dynamic | **Implemented (Phase 5 step 12 family a).** The slot starts as `undefined`; TypeScript still rejects a typed read before a write. |
| Object destructuring | static | static if source object is typed, else dynamic | **Implemented (Phase 5 step 12 family a).** A shallow pattern of identifiers desugars to field reads. Nested patterns, rename rest (`...r`), and defaults in the pattern stay not-yet. |
| Array destructuring | static | static if source array is typed, else dynamic | **Implemented (Phase 5 step 12 family a).** A shallow pattern of identifiers desugars to index reads. Nested patterns, rest, and holes-with-defaults stay not-yet. For-of destructuring stays Phase 6. |
| `try`/`catch`/`finally`/`throw` | static | static; catch binding is `Unknown` in `.js` without JSDoc type | **Implemented (Task 3.10).** Exception unwinding via landing pads (docs/VALUE.md §4.9): `jsrt_pending()` checks after every call, a per-try completion code routes `return`/`break`/`continue` through `finally`. The catch binding is `Unknown` in BOTH modes — anything can be thrown — so reading it is a narrowing point; a shallow identifier pattern on the catch binding is implemented (Phase 5 step 12 family a). An uncaught exception prints `Uncaught <value>` on stderr and exits 1. |
| `async` function declarations and expressions, `await` | static | static if typed, else dynamic | **Implemented (Task 4.6).** The body lowers to a state machine: one C function re-entered at a numbered resume point, with the locals that outlive an await living in a heap environment rather than on the stack. `await` is a suspend/resume pair over `jsrt_await` — it subscribes a native continuation to `jsrt_promise_resolve(operand)`, so awaiting a non-promise is legal and still costs exactly one tick, which is what makes an interleaving match Node's rather than merely finishing with the same answer. The prefix up to the first await runs synchronously on the caller's stack (observable, and the difference between an async function and a callback). A throw that escapes the body rejects its own promise via the landing pad, so `try`/`catch` around an `await` works with no new protocol. `main` drains the queue once after the module body: there are no timers and no I/O, so there is no macrotask phase. Deferred: `for await`, async generators, and async arrow/method forms not covered by the declaration and expression shapes. |
| Generator functions and `yield` | static | static if typed, else dynamic | **Implemented (Phase 5 step 8).** A `function*` is Task 4.6's resume machine driven by the caller: `gen()` only allocates, the body runs on the first `next()`, and `yield e` parks `e` then pops the C frame. A later `next(v)` is the value of that `yield`. Locals live in the heap environment for the same reason an async function's do. `for-of` over the generator discards the completion value; `next()` after `return` answers `{ value, done: true }`. Uncaught throws stay pending for the call site of `next()`. `.return(v)`/`.throw(e)` re-enter the parked body with an injection rather than a value: `throw` rethrows at the yield's own landing pad (the body's `try`/`catch` sees it exactly as if the `yield` threw), and `return` parks `v` and routes through every enclosing `finally` the way a real `return` at that point would — a `finally` that yields delays the answer, exactly per ECMA-262 27.5.1.3. On an unstarted or completed generator neither enters the body: `return(v)` answers `{ value: v, done: true }`, `throw(e)` rethrows to the caller. Deferred: generator methods and async generators (**STA1201**, receiver-in-env), `yield*` (**STA1214**), and `return`/`throw` on a boxed SPECIALIZED iterator (`arr.keys()`), whose object does not carry them (Node answers a TypeError the runtime cannot raise yet). |

---

## Types & typing policy

| Feature | `ts` mode | `js` mode | Notes |
|---|---|---|---|
| Type annotations (`: number`, `: string[]`, etc.) | static | static (where present) | Types are strictly required in `ts` mode; optional in `js` mode (inferred from code or JSDoc). |
| Interfaces and type aliases | static | static (where present) | Compile-time constructs; no runtime representation. |
| Explicit `any`, `as any` | error(STA1001) | dynamic | `ts` mode forbids `any` entirely (use `unknown` instead); `js` mode lowers to tagged values with dynamic dispatch. |
| Implicit `any` | error(STA1003) | dynamic | `ts` mode rejects untyped parameters and variables under strict mode; `js` mode lowers to `Unknown` (tagged, dynamic). |
| Union types | dynamic (tagged; narrowing = runtime check) | dynamic | **Implemented (Task 3.5), by having no union at all.** The HType model has no union node, so `string \| number` IS `Unknown` — and narrowing one therefore lands on exactly the machinery an `unknown` already needed, with no separate feature. A union whose constituents all map to ONE HType is that type instead: `"a" \| "b"` is a `string`, which is what makes `typeof` usable (its own type is a union of eight string literals). Discriminated unions checking a discriminant TAG are still deferred — that needs a union the model can see the constituents of. |
| `unknown` type | dynamic (narrowing = runtime check) | dynamic | **Implemented (Task 3.5).** A narrowed read emits `jsrt_check_number/string/boolean`, which returns the value or raises `STA2001` with a `file:line:col`. The check is per USE, not per binding: nothing proves the value did not change between two reads. An UNnarrowed `unknown` gets no check — printing one asks nothing of it — and a narrowing to a type no tag settles in constant time (an object, an array, a signature) leaves the value `unknown` on the dynamic path rather than being refused. |
| `as` casts | dynamic (boundary-checked) | dynamic | **Implemented (Task 3.5).** A cast off an `unknown` to a checkable type emits `jsrt_check_*`. An identity cast and a widening to `unknown` assert nothing and lower to the operand alone. A cast to a type no tag settles is DROPPED rather than believed: the value stays `unknown` and every operation downstream stays on the dynamic path, which is sound and costs nothing that compiles today. The older `<T>x` spelling is not accepted — it is ambiguous with JSX and adds a second syntax for a construct that has one. |
| `JSON.parse()`, FFI returns | dynamic (boundary-checked, always) | dynamic | **`JSON.parse` implemented (Task 4.2, tenth slice)** — see its own row below; it lands `unknown`, and the checks its uses need are exactly the ones `as` and narrowing already emit. FFI returns are still Phase 6. |
| Generic function declarations | static (monomorphized per concrete type tuple) | static, identically — a `.js` file cannot spell a type parameter, so a js-mode generic is a `.ts` file and gets the same specializations | Specialization happens AT the lowering, so no type parameter ever enters the HIR. Instantiations are shared by HType identity, which collapses literal types for free: `box(1)` and `box(2)` are one function. Depth is capped at 16 (STA2003) — `f<T>` calling `f<T[]>` has no fixed point. |
| Generic function used as a value, generic arrows, constrained or defaulted type parameters, explicit type arguments, generic classes | not-yet(STA1214) | not-yet(STA1214) | Each has no tuple to specialize on, or no declaration to lower a second time. A value has one identity and many instantiations; a constraint is a check the subset cannot make. |

---

## Objects & classes

| Feature | `ts` mode | `js` mode | Notes |
|---|---|---|---|
| Classes with fixed shape (no getters/setters) | static (fixed slot offsets) | static if fully typed, else dynamic | **Implemented (rung 6a).** An instance is one allocation: a pointer to a file-scope `JSRTClass` descriptor followed by its slots, with declaration order as slot order. A field read is an offset load, not a lookup. The slot list comes from the CHECKER's property list, not from the member nodes, which is why a `.js` class — whose fields are declared by `this.x = …` in the constructor and have no member node at all — gets the same layout with `unknown` field types. Methods occupy no slot: one function is shared by every instance and the call resolves at compile time to that class's method, so `o.m()` is a direct call with the receiver as argument zero. Statics, `#private`, inheritance, overriding, accessors and object literals have all landed on top of this layout — see the rows below, each of which says what it reduced to rather than what it added. |
| Classes with getters/setters | static (the accessor is a call; the fields keep their slots) | static if typed, else dynamic | **Implemented (rung 6b).** An accessor is a pair of METHODS under a name no source can spell — `get x`, `set x`, where the space does what the dot does for a static. So `o.x` is a call to `get x`, `o.x = v` is a call to `set x`, and the property occupies no slot, which is exactly what the property MEANS. The reduction is why accessors needed no HIR node, no verifier case and no emitter case: they inherit dispatch, the method table, arity padding and the receiver parameter unchanged, and `util.inspect` never prints them because there is no slot to print. This is a correction to what this row used to claim — an accessor does NOT force the class onto the dynamic path, and the other fields keep their fixed slots (plan-notes 69). Deferred: a compound assignment to an accessor (`o.x += 1` is a get and a set of one property, and what evaluates the receiver once across the pair hoists a slot), a static accessor (it belongs to the class object, which a plain binding is not), a `#private` or computed accessor name, and overriding an inherited accessor. |
| Class inheritance and `super(...)` | static (prefix layout; direct dispatch) | static if types are present, else dynamic | **Implemented (rung 6b).** A subclass's slots START with its base's, in the base's own slot order, so a base-typed read of a subclass instance lands on the right slot and a `Dog` is assignable wherever an `Animal` is declared. The slot list is rebuilt root-first from the chain — the checker lists a subclass's OWN properties first, which is the opposite of what a prefix layout needs — and a name an ancestor already claimed keeps the ancestor's slot, which is what makes a `.js` field assigned in both the base and the subclass one slot rather than two. `super(...)` is the base constructor run against the receiver this one was handed; a derived class that writes no constructor gets JavaScript's implicit `constructor(...args) { super(...args) }`, taking the nearest declared ancestor constructor's parameters. Field initializers run AFTER `super(...)`, so an initializer may read a field the base wrote. A method call names the class that DECLARES the method, so an inherited method is a direct call to the one function that exists. |
| Method overriding and `super.method()` | static (method-table dispatch) | static if types are present, else dynamic | **Implemented (rung 6b).** One name, one slot, a different entry per class: a subclass's method table BEGINS with its base's, in the base's order, so a slot resolved against a receiver's static type indexes the right method on every descendant. A call is virtual exactly where it has to be — the lowering asks the whole file whether any chain containing the receiver's class declares that method twice, so a method nothing overrides keeps rung 6a's direct call and costs nothing. `super.m()` is a call on the SAME receiver that skips the override, which is why it stays direct even where every other call to `m` is virtual; a virtual call there would find the override again and recur. The table lives in the class descriptor and its entries are file-scope closure constants, which is what forces the two deferrals: overriding inside a function (a method there may capture, and a captured environment is per evaluation, so there is no one table) and re-declaring a FIELD (a field is a slot, and two declarations of one slot have two initializers racing for it). `super.x` on a field is refused for the sharper version of that: `super.x` and `this.x` are the same slot. In `js` mode `noImplicitOverride` is off — it would demand a JSDoc `@override` tag on ordinary JavaScript — while `ts` mode keeps it, since there the modifier is real syntax and an accidental override is exactly what a table makes silent. |
| Static methods and static class members | static | static if typed, else dynamic | **Implemented (rung 6b).** A static belongs to the class object, not to any instance, so it is not a slot: it is ONE binding for the whole program, under a name no source can spell (`C.count` — the dot is what makes it unspellable, the same trick the receiver parameter plays with a leading space). That reduction is why statics needed no HIR node, no verifier case and no emitter case of their own — a static read is an identifier, a write is an assignment, and `C.m()` is an ordinary call. The binding is named by the DECLARING class, because statics are inherited: `Sub.count` and `Base.count` are the same static, and mangling by the receiver's spelling would make a write through one invisible through the other. The initializer runs where the class declaration sits, and forward references between statics work for the reason function declarations hoist. Deferred: a `static {}` initialization block, and `this` inside a static member — both need the class object this model does not build. |
| Private fields (class `#field` syntax) | static | static if typed, else dynamic | **Implemented (rung 6b).** A `#private` member is an ordinary member: `#count` takes a slot, `#step()` is a member function, `static #next` is a static binding. Privacy is not a layout property — the checker has already rejected every access from outside the class body, so nothing below the gate has to enforce it. The one place it survives is printing: `util.inspect` omits `#private` fields, so the runtime printer skips a descriptor field name that starts with `#`, and a class whose fields are ALL private prints as `C {}`. The `#` cannot collide with a public name because no public property may be spelled with one. Deferred: a subclass re-declaring an ancestor's `#private` name (two distinct slots that share a spelling, which a layout keyed by name cannot hold apart), and `#brand in o`, which asks whether an object carries the slot at all rather than reading it. |
| Object literals with static keys | static (fixed slot offsets) | static if all values are typed, else dynamic | **Implemented (rung 6b).** A literal is a CLASS whose declaration is a type: the shape comes from `checker.getTypeAtLocation`, and the entries the source wrote are its slots, in written order. That is the whole reduction — the same `JSRTClass` descriptor, the same allocation, the same offset load for `p.x`, and no HIR node beyond `ObjectLiteral` itself. Two literals with the same field names and types get the same STRUCTURAL descriptor name (`{x: number, y: string}`, whose leading brace is what no source identifier may start with), so one descriptor is emitted and shared; a different key ORDER is a different shape, because order is layout. The descriptor's name is the empty string, and the printer treats an empty name as "no constructor name", which is what makes `{ x: 1 }` print without a prefix where a class instance prints `Point { x: 1 }`. Deferred: a shorthand, spread, method or accessor member, and a key that is not an identifier. A literal whose type has an optional property or an index signature no longer waits — it takes the DYNAMIC path in the next row. |
| Object literals with optional properties, index signatures | dynamic (shape table + inline cache) | dynamic | **Implemented (Task 4.1).** The CONTEXTUAL type decides: in `const o: { x?: number } = { x: 1 }` the literal's own type is a layout, but every later read of `o` goes through the annotation, so the literal builds a `JSRTDynObject` and each property SITE resolves the name through the shape table with its own inline cache (docs/VALUE.md §4.10). Reads of an absent property answer `undefined`; every read types `Unknown` and narrows like a `Map.get`. Plain `=` writes only — the compound forms fold to a read of the place, and the read-once machinery hoists slots, which a shape-table entry is not. In js mode the shape can come from JSDoc (`@type {{x?: number}}`), which the checker honors in a `.js` file. Honesty clause: structural aliasing can hand a FIXED-shape object to a dynamic site (`const a = { x: 1 }; const b: { x?: number } = a; b.x`). A read or write of an existing field walks the class descriptor and answers; growing a new key (`b.y = 2`) still aborts with `STA2004` (Phase 8) rather than invent a slot. An empty `{}` is dynamic so it can grow. An Unknown receiver (`function f(o) { return o.x }`) uses the same shape-table path, including computed index and call (a non-function callee is `STA2006`). Deferred: a computed key (`{ [k]: v }` — the key is a runtime value, and every key here is a compile-time name), method calls through the shape, and `delete`. |
| Getters/setters on object literals **[proposed]** | dynamic | dynamic | Accessor descriptors cannot be compiled to fixed field access; property access routes through the descriptor. |

---

## Collections & data structures

| Feature | `ts` mode | `js` mode | Notes |
|---|---|---|---|
| Arrays: dense, homogeneous element type | static (dense buffer, bounds-checked; elision when provably in-range is Phase 3 Task 3.5) | static if typed, else dynamic | `[…]` literals, `.length`, `for-of` and index WRITE are all static. js mode is not "always dynamic": `[1, 2, 3]` infers `number[]` with or without the annotation, so it compiles on the same path. |
| Arrays: index READ `a[i]` | dynamic | dynamic | `noUncheckedIndexedAccess` types the read `T \| undefined`, because out of range it really is `undefined`. That union is `Unknown`, and Task 3.5 (boundary-check insertion) is what narrows it back to `T`. `for (const x of a)` binds the ELEMENT type and stays static — it is the typed way to read an array today (plan-notes 53). |
| Arrays: sparse (a write more than one past the end) | not-yet(STA2002, raised at runtime) | not-yet(STA2002, raised at runtime) | ECMA-262 leaves the skipped indices absent — `console.log` prints `<2 empty items>`, not `undefined` — and a dense buffer cannot be absent, so the write refuses loudly rather than printing a different program's output. In-range writes and `a[a.length] = v` are unaffected. Lifts with the object model. |
| Arrays: heterogeneous or untyped | dynamic (element type `Unknown`, tagged values) | dynamic | Elements stored as tagged `jsrt_value`; shape check on access to narrow type. |
| `Map` with primitive keys (string, number, boolean, null, undefined) | static (open-addressed hash table; `get`/`set`/`has`/`delete`/`clear`/`size`) | static if typed, else dynamic | **Implemented (rung 7).** A Map and a Set are ONE structure under two `JSRTClass` descriptors — `jsrt_class_map` and `jsrt_class_set`, distinguished by descriptor pointer the way object literals are — because the value half is the only difference between them and SameValueZero is the only comparison either needs. There is no separate "specialized primitive" table, and this row used to claim one: a NaN-boxed primitive key already IS its unboxed bits and an object key already IS its pointer, so one comparison serves both key kinds and two implementations would have been two copies of the same code (plan-notes 72). A key of `-0` is normalized to `+0` on the way IN, per the spec's own step in `set`/`add` — comparison could never show the difference, but the key a `forEach` callback receives can (plan-notes 98). Entries are appended to a dense array and indexed by an open-addressed probe table kept at load ≤ ½, so INSERTION ORDER is iteration and print order — including across a delete-then-reinsert, which appends at the end rather than reclaiming the hole, and across the growth compaction. The type arguments must be on the CONSTRUCTION: `const m: Map<string, number> = new Map()` types the call itself `Map<any, any>`, which is Unknown, so only `new Map<string, number>()` is static. `.get` is dynamic wherever it appears — `noUncheckedIndexedAccess` aside, the lib types it `V \| undefined`, and that union has no HType. `forEach` landed with the Task 4.2 addendum (2026-08-30): it takes a CALLBACK, not an iterator, and the runtime walks the entry array calling it through `jsrt_call` — the same closure ABI the `Array.prototype` callback ops already use, so it needed no protocol the subset lacks. It passes the spec's triple (`(value, key, collection)`; for a Set the value IS the key), stops on a pending exception, and sees mutation during the walk the way the spec requires: an entry deleted before it is reached is skipped, one appended during the walk IS visited, and a `clear()` ends the walk. That last part is why the table suppresses its growth COMPACTION while a walk is in flight (`JSRTMap.iterating`) — compaction renumbers entries, and the walk holds an index. The thisArg form is deferred like every other callback op's. `keys`/`values`/`entries` land with Phase 5 step 8 (plan-notes 150): for-of inlines; a stored iterator boxes. Map `entries()`/`for-of` yield a two-element array, so a file that uses the pair is dynamic (no tuple). Deferred: constructing from an iterable (`new Map([['a', 1]])` — the user-iterable protocol), and `WeakMap`/`WeakSet`, which need the collector to have a notion of weakness. |
| `Map` with object or unknown keys | dynamic (the KEY TYPE is Unknown; the table underneath is the same one) | dynamic | **Implemented (rung 7).** Not a separate path: SameValueZero on a non-primitive IS pointer identity, so two instances with identical fields are two keys and the table needs no object model to say so. The verdict on the FILE still turns on the type — `Map<object, V>` is Unknown, because `object` describes no layout — but that is the type model talking, not the table. |
| `Set` with primitive elements | static (the Map table with the value half unused) | static if typed, else dynamic | **Implemented (rung 7).** `add` rather than `set`, and one argument rather than two; everything else is the row above. The seven ES2025 set operations landed with the Task 4.2 addendum (2026-08-30): `union` `intersection` `difference` `symmetricDifference` answer a NEW Set (neither operand is touched), and `isSubsetOf` `isSupersetOf` `isDisjointFrom` answer a boolean. Their argument is the only place in the subset where a collection operation takes another collection, and it must be a real `Set`: the spec accepts any SET-LIKE object (a `size`, a `has`, a `keys`) and reads it by CALLING `keys()`, which is the iterator protocol still deferred — so a set-like object is `not-yet(STA1214)` while a Set is read straight out of the second table. Result order is normative and is not always the receiver's: `intersection` walks whichever collection is smaller and answers in THAT one's order, which the spec spells out and the pinned Node observes (plan-notes 100). |
| `Set` with object elements | dynamic (the ELEMENT TYPE is Unknown; the table is the same one) | dynamic | **Implemented (rung 7).** See the two rows above — the element kind changes nothing about the structure. |
| `Promise.resolve`, `Promise.reject`, `Promise.all` | static | static | **Implemented (Task 4.6).** The three statics that never call back, which is why they land while the constructor does not. `Promise.resolve` of a promise answers the SAME promise (§27.2.4.6, identity preserved); of anything else, an already-fulfilled one. `Promise.all` fills its result BY INDEX rather than by arrival, so input order survives however the elements settle, a plain value in the list counts as already-resolved, an empty list settles on the spot, and the first rejection wins (later settles are ignored because settling is idempotent). Its argument must be an array — an iterable is the deferred iterator protocol — and a rejection with nothing subscribed to it aborts at the end of the drain (`STA2005`) rather than being swallowed. |
| `new Promise(executor)`, `.then`/`.catch`/`.finally` | static | static | **Implemented (Phase 5 step 11).** `jsrt_call_protected` turns a pending throw into a completion value so a handler's throw rejects the derived promise instead of unwinding into library C. `then`/`catch`/`finally` are clients of `jsrt_promise_subscribe`. `new Promise(executor)` runs the executor now; a throw rejects the constructed promise; a non-function executor throws TypeError. Combinators (`allSettled`/`any`/`race`/`withResolvers`/`try`) stay not-yet. |
| `new Date(ms)`, `new Date(isoString)`, `new Date(other)`, `new Date()` | static | static | **Implemented (Task 4.2, slice A).** A Date is one `double` on the heap -- milliseconds since the epoch, or NaN for an Invalid Date -- behind a `jsrt_class_date` descriptor, so every member below is a direct runtime call and nothing about it is dynamic. One C entry point (`jsrt_date_from_value`) covers all three argument forms because the discrimination is a tag test; anything else becomes an Invalid Date, which is what the spec's ToPrimitive-then-ToNumber path amounts to. TimeClip (§21.4.1.31) applies: a magnitude past 8.64e15 is an Invalid Date. The ZERO-argument form reads the clock and is accepted -- nondeterminism is a proof problem, not an acceptance problem -- and the lowering desugars it to `new Date(Date.now())`, which §21.4.2.1 step 2 defines it as; its proof is `tests/unit/date-clock.test.ts`, not a golden fixture. The COMPONENT form `new Date(y, m, …)` is local time and landed with slice B (row below). |
| `Date.prototype` UTC surface: `getTime`, `valueOf`, `setTime`, the 8 `getUTC*`, the 7 `setUTC*`, `toISOString`, `toJSON`, `toUTCString` | static | static | **Implemented (Task 4.2, slice A).** The TZ-INDEPENDENT core, on the `DATE_OPS` table discipline: every member is a pure function of the stored double, so the answer on any machine is the answer on the pinned Node -- which is what makes the whole slice golden-testable without a `TZ` pin. Calendar arithmetic is Hinnant's `days_from_civil`/`civil_from_days` over the proleptic Gregorian calendar §21.4.1 defines, so 1900 is not a leap year, 2000 is, and pre-epoch times floor rather than truncate. Setters take their trailing components optionally (the lowering pads with `undefined`, which the spec's "if present" step reads identically) and ROLL rather than clamp; `setUTCFullYear` on an Invalid Date recovers per §21.4.4.21 while every other setter leaves it invalid. `toJSON` answers `null` for an Invalid Date (§21.4.4.37) even though `lib.es5.d.ts` declares it `(): string` -- a **lib divergence**, and what makes `JSON.stringify(new Date(NaN))` the string `"null"` rather than an abort. `console.log(d)` prints the ISO string, nested included; `JSON.stringify` routes through `toJSON`. **Known ceiling:** `toISOString` on an Invalid Date throws a `RangeError` (Phase 5 step 11); generated C checks `jsrt_pending()` after the op. |
| `Date.UTC`, `Date.parse`, `Date.now` | static | static | **Implemented (Task 4.2, slice A).** `Date.UTC` takes 1-7 arguments -- the spec defaults month to 0, date to 1 and the rest to 0, filled at LOWER time so the runtime entry stays fixed-arity, the Math-fold model -- and applies the §21.4.3.4 two-digit-year rule (0-99 means 19xx). `Date.parse` accepts the §21.4.1.32 Date Time String Format ONLY; anything else answers `NaN`. That is a **documented divergence from Node**, which applies non-ISO heuristics whose results are TZ-dependent and implementation-defined; a date-time with no offset is local time and answers `NaN` for the same reason, and neither is exercised in a golden fixture. `Date.now` reads the wall clock and lands under the determinism carve-out with the zero-argument constructor. |
| `Date.prototype` local surface: the 8 local getters, `getTimezoneOffset`, the 7 local setters, `toDateString`, `new Date(y, m, …)` | static | static | **Implemented (Task 4.2, slice B).** Same calendar arithmetic as the UTC rows over a reading shifted by the host zone: `local_time(t) = t + LocalTZA(t)` with the offset from libc `localtime_r`, which OWNS the tzdb -- the runtime carries no zone rules of its own. The setters and the component constructor need the inverse, and the inverse is not a bijection: a wall-clock reading in a DST gap names no instant and one in a fold names two. §21.4.1.26 resolves both with the offset in effect BEFORE the transition -- the earlier instant when there are two -- which the runtime finds by probing the offset one day either side, the spec's own window. `getTimezoneOffset` reports minutes WEST, so UTC+2 answers -120. `toDateString` is here rather than with the ICU family because its output carries no zone name. **Proof split:** the golden runner pins `TZ=UTC` on both sides (the compiled binary AND the Node ground truth), which is what stops a tzdata skew between libc and Node's ICU from surfacing as a byte diff that looks like a semantics bug; under that pin the goldens prove the wiring and the arithmetic, and every zone-dependent claim -- gaps, folds, half-hour offsets, transitions a setter crosses -- is proved in `tests/unit/date-local.test.ts` under an explicit non-UTC `TZ`. |
| `Date.prototype.toString`, `toTimeString`, `toLocaleString`, `toLocaleDateString`, `toLocaleTimeString`, the call form `Date()` | not-yet(STA1210, feature build) | not-yet(STA1210, feature build) | All that is left under `STA1210`, and it carries **no phase** (2026-09-01, plan-notes 136): a feature build is a flag to turn on, not a release to wait for. `toString` and `toTimeString` are NOT locale-dependent but are ICU-dependent all the same: both append the host zone's long display name (`Mon Jul 15 2024 14:00:00 GMT+0200 (Central European Summer Time)`), which Node reads from ICU and libc cannot produce -- `%Z` gives the abbreviation `CEST` (measured, plan-notes 133). So they join the `toLocale*` family on Task 4.4's `intl_*` feature surface -- non-blocking, the same shape as the rest of the flag build (`STA1215` model). |
| `d2 - d1` | error(STA0012) | dynamic | Date ARITHMETIC needs no rule of this compiler's in `ts` mode: it relies on §21.4.4.45's `[Symbol.toPrimitive]` hint, `lib.es5.d.ts` types both operands `Date`, and strict TypeScript refuses the operator before the gate is asked. The sanctioned spelling is `d2.getTime() - d1.getTime()`. `[Symbol.toPrimitive]` itself stays out: Symbol-keyed members are out of every surface list. |
| `RegExp` literals | static | static | **Implemented (Task 4.3).** `/ab+c/gi` lowers to a `regexp-literal` carrying the pattern and the flags as TEXT: nothing above the C boundary parses either, so nothing above it can disagree with the vendored engine (quickjs-ng's libregexp, `runtime/vendor/quickjs-ng`) about what they mean. Compiled at EVERY evaluation, never hoisted — §22.2.4.1 makes each evaluation a fresh object, and it has to be, because `lastIndex` is mutable state on it. An invalid pattern or flag string is settled where it is compiled: at run time, loudly (`STA2005` pattern), because the spec throws SyntaxError and builtins cannot raise yet. `new RegExp(...)` is a separate not-yet — a pattern that is not in the source is a pattern the compiler cannot see. |
| `RegExp.prototype.test` | static | static | **Implemented (Task 4.3).** The whole landed surface, on the `REGEXP_OPS` table discipline: one runtime function with a fixed C signature, a callee only, never a value. `lastIndex` is read and written by a `/g` or `/y` pattern and reset to 0 on a failed match, which is what makes `while (re.test(s))` terminate. The subject follows JSON.parse's rule — a string, or untyped and settled by the bridge's own tag check (`STA2005`), which is the js-mode norm. Coverage: same dashboard. |
| `RegExp.prototype.exec`, `String.prototype.match` | dynamic | dynamic | **Implemented (Task 4.1, array-with-properties slice).** `exec` answers the MATCH ARRAY: a dense array of the capture groups that also carries `index`, `input` and `groups` as named properties — so `JSRTArray` gained the dynamic object's property table (`shape` + out-of-line slots, NULL until something hangs a property off it), and a match read resolves through the same shape chain and the same per-site inline cache a `{ }` receiver uses. `groups` is a NULL-PROTOTYPE object where the pattern declares named captures and `undefined` where it does not, and it PRINTS as one (`[Object: null prototype] { w: 'a' }`) because Node does. A capture that did not participate is `undefined` in the array, not a missing element. `lastIndex` moves exactly as `test` moves it — exec and test are one algorithm with two answers. `match` without `/g` IS exec; with `/g` it answers a plain array of whole-match strings, built by CreateArrayFromList and therefore carrying no properties, or `null`. The verdict is **dynamic**, not static: the answer is a match OR null, a union the HIR does not model, so the binding is Unknown even though every read off it lowers to a fixed runtime call. |
| A match array's members | dynamic | dynamic | Exactly four names: `index`, `input`, `groups` (properties of the match) and `length` (the array header's own). The receiver's HIR type is Unknown, so what proves a receiver is a match is the CHECKER — `isMatchReceiver` in `src/frontend/gate.ts`, the discipline `isStringReceiver` follows — and the runtime settles it a second time, because a tag `jsrt_get_prop` cannot walk is a panic rather than a misread. `m[0]` indexes it like the dense array it is. Everything else on a match (`m.map`, `m.slice`, spreading it) is `not-yet(STA1214, Phase 5)`: it waits for the match array to have an HIR type of its own, which is the union work. |
| `RegExp.prototype` data properties, `toString` | static | static | **Implemented (Task 4.2).** `source`, `flags`, `lastIndex` and the eight flag predicates, on a closed table of their own (`REGEXP_FIELDS`) beside the method table. Every one is DERIVED from the compiled regexp — two normalized strings, a header field, and one bit test each — so a read is a struct load behind a fixed C signature, never a shape-table lookup, and it is static even in js mode. `flags` is the spec's canonical `dgimsuvy` ORDER, not the order the source wrote: `/a/ig` has `.flags === "gi"` and prints `/a/gi`, and the runtime normalizes once at construction so `flags`, `toString` and `console.log(re)` cannot disagree. `lastIndex` is READ-only: a write is an assignment target, which the assignment gate does not admit for a builtin. `unicodeSets` is in the table but UNREACHABLE — it is declared in `lib.es2024.regexp.d.ts` and this project's `lib` is es2023, so the checker cannot type the read. |
| `RegExp.prototype.compile` | not-yet(STA1211, Phase 8) | not-yet(STA1211, Phase 8) | The one member outside both tables. Annex B §B.2.4 legacy that RE-INITIALIZES a regexp in place -- the mutate-a-built-object surface Phase 8 owns with `STA1204`, which is why it left Phase 4 on 2026-09-01 (plan-notes 121, 136). Its optional second argument, which the fixed-arity op table cannot express, is a second reason. |
| `Symbol` primitive **[proposed]** | not-yet(STA1212, Phase 5) | not-yet(STA1212, Phase 5) | Well-known symbols as *values*, `Symbol()` / `Symbol.for` / `Symbol.keyFor`, and the registry. `[Symbol.iterator]()` as a class method name is admitted for user-iterable `for-of` (Phase 5 step 8) without landing the primitive. |
| `BigInt` primitive **[proposed]** | not-yet(STA1213, Phase 5) | not-yet(STA1213, Phase 5) | Separate numeric type with dedicated arithmetic. Deferred past MVP. |

---

## Numbers & operators

| Feature | `ts` mode | `js` mode | Notes |
|---|---|---|---|
| `number` type, `i32`/`f64` split per `NUMERIC.md` | static (i32 where range-provable; overflow to f64; all double by default) | static / dynamic | Typed numbers compile to machine integers and doubles; untyped numbers use dynamic boxing. |
| `string` type | static | static / dynamic | Typed strings are `JSString` pointers; untyped strings use tagged `jsrt_value`. |
| `boolean` type | static | static | Booleans always unboxed (1-bit tag in NaN-box). |
| Arithmetic operators: `+`, `-`, `*`, `/`, `%` **[proposed]** | static (where operands are typed) | static if typed, else dynamic | Typed numeric operands use fast C arithmetic; untyped route via `ToNumber()` coercion. |
| Unary `+`, `-`, `~` (bitwise NOT), `!` (logical NOT) **[proposed]** | static | static if operand is typed, else dynamic | Negation and bitwise complement depend on operand type knowledge. |
| Bitwise operators: `&`, `\|`, `^`, `<<`, `>>`, `>>>` **[proposed]** | static (apply `ToInt32`/`ToUint32` per ES spec) | static if typed, else dynamic | Always apply spec-mandated integer coercion; `>>>` produces uint32, may need f64. |
| Exponentiation operator `**` **[proposed]** | static | static if typed, else dynamic | Lowers to `pow()` call; type-driven. |
| `==`, `!=` loose equality | static where operand types decide it, else dynamic (full ToPrimitive coercion on the dynamic path) | same | Typed operands (primitives or disjoint types) compile to direct comparison; untyped route via `jsrt_equal_loose()`. |
| `===`, `!==` strict equality **[proposed]** | static | static | Unboxed comparison (pointer equality for objects, value equality for primitives). |
| Comparison operators: `<`, `>`, `<=`, `>=` **[proposed]** | static where types allow, else dynamic | same | Typed primitives use C comparison; untyped route via `ToPrimitive()` and `jsrt_compare_*()`. |
| Logical operators: `&&`, `\|\|` **[proposed]** | static (control flow; short-circuit optimization) | static (same) | Compiled to conditional branches. |
| `typeof` operator | static | static | **Implemented (Task 3.5).** Its own HIR node, not a `UnaryOp`: it coerces nothing (it is total on every value, where every other prefix operator runs ToNumber or ToBoolean) and its result is a `string`, where `UnaryOp`'s is fixed to number or boolean. Not constant-folded — `typeof` asks the VALUE, and a value whose static type is `number` may still be an unchecked `unknown` underneath, which is the entire reason a guard is worth compiling. |
| `instanceof` operator | static | static | **Implemented (rung 6b).** The right operand must be a class NAME, not an expression: there is exactly one `JSRTClass` descriptor per class in the program, so the test is a pointer comparison against it and `x instanceof (cond ? A : B)` has no descriptor to name. The left operand is anything at all — every primitive answers `false`, and so does an array or a function, which are objects carrying no descriptor. Each descriptor carries its base's, so the test walks the parent chain — that walk lives entirely inside `jsrt_instanceof()`, which is why inheritance landing changed no generated C at the call site. |
| Optional chaining `?.` **[proposed]** | static (compiles to null/undefined check) | static (same) | Null-check short-circuits the rest of the chain. |
| Nullish coalescing `??` **[proposed]** | static (compiles to null/undefined check) | static (same) | Binary operator; RHS evaluated only if LHS is null/undefined. |

---

## Globals & builtins

| Feature | `ts` mode | `js` mode | Notes |
|---|---|---|---|
| `console` (15 members): `log` `info` `debug` `warn` `error` `dir` `group` `groupEnd` `count` `countReset` `assert` `table` `time` `timeEnd` `trace` | static | static | One HIR node per call, not a property lookup on a `console` object -- there is no `console` value to look anything up on. `CONSOLE_METHODS` in `src/hir/nodes.ts` is the single table the gate, the lowering, the verifier and the emitter all read: each member declares its arity, how many of its trailing arguments are optional, and the C entry point. An omitted optional argument pads to `undefined` at lowering only where explicit `undefined` means what absence means -- true of `count`/`countReset`, false of `group`/`assert`, whose short forms get their own C entry point instead (Node prints `console.group(undefined)` as `undefined` and `console.assert(c, undefined)` as `Assertion failed undefined`, where the omitted forms print nothing). `log`/`info`/`debug` inspect onto STDOUT and `warn`/`error` onto STDERR -- Node's own split, which the golden runner holds to byte-for-byte by comparing both streams. `dir` is `log`'s formatting WITHOUT its one exception, so a top-level string keeps its quotes. `group`/`groupEnd` maintain a runtime indent that prefixes EVERY line of a multi-line inspect, and an unmatched `groupEnd` is a no-op, as in Node. `table` (landed 2026-09-01) draws Node's box grid: a leading `(index)` column, one column per key seen across the rows in FIRST-SEEN order, and a trailing `Values` column for rows that are not objects. Cells are inspect form -- a string cell is quoted, where the index label is not, because a key is not a value -- a missing key leaves the cell empty rather than printing `undefined`, an ARRAY row contributes its indices as columns, and a value that is not a collection of rows falls back to `log`, which is Node's own rule. Padding is by DISPLAY width, so a CJK cell is two columns per character. Its layout is a pure function of the data, which is what separates it from the three below. Deferred: `console.table` on a **Map or Set**, which Node draws with an `(iteration index)` column -- and a Map with a second `Key` column -- a different table rather than a wider one (`STA1214`). Under the **determinism carve-out** (plan-notes 116, 124, 127), `time`/`timeEnd`/`trace` **landed 2026-09-01** with a shape assertion rather than a golden test, because a duration measures this machine on this run and a stack is frames this runtime has no unwinder to produce. `time` prints nothing and keeps the ORIGINAL start when a running label is re-timed; `timeEnd` prints `label: <duration>` on stdout using Node's own unit ladder (ms below a second, `s` below a minute, `m:ss.mmm` above one) and prints NOTHING for a label that was never started, which is what Node writes to stdout for that case; `trace` prints `Trace[: message]` on STDERR without frames -- the same decision `jsrt_uncaught` already made, that inventing frames is worse than omitting them. Proof: `tests/unit/console-carveout.test.ts` (the label is echoed, a duration follows it, the unit is `ms`, more work measures longer, and the stream split holds). |
| `undefined` | static | static | Not a global reference at all: it lowers to an undefined-literal. A user binding that shadows the name wins, exactly as it does at runtime. |
| `Math` (exactly-specified operations): `abs` `ceil` `floor` `round` `sign` `sqrt` `trunc` `pow` `min` `max` `clz32` `imul` `fround`; approximated operations `acos` `acosh` `asin` `asinh` `atan` `atan2` `atanh` `cbrt` `cos` `cosh` `exp` `expm1` `log` `log10` `log1p` `log2` `sin` `sinh` `tan` `tanh`; binary `hypot`; `random`; all 8 constants; globals `NaN`, `Infinity` | static | static | **Implemented (Task 4.2).** Number → number, ECMA-262 §21.3.2 exactly, with one `jsrt_math_*` C function per operation. The wrappers preserve the named ECMA/libm edge cases (`round`, `sign`/`min`/`max`, and `pow` special cases). Variadic `min`/`max` fold left into nested binary nodes; `hypot` accepts zero/one/two arguments with the specified degenerate forms and intentionally gates larger arities because it is not associative. The implementation-approximated operations use the vendored V8 fdlibm port, avoiding host-libm last-ulp drift. `Math.random` is nondeterministic by specification and is proved by range/distribution assertions in `tests/unit/math-random.test.ts`, not byte-for-byte golden output. Constants and `NaN`/`Infinity` fold to number literals. Coverage: `tests/golden/builtins_coverage.json`, rendered by `pnpm run test:builtins`. |
| `String.prototype` (39 methods): `charAt` `charCodeAt` `indexOf` `lastIndexOf` `includes` `startsWith` `endsWith` `slice` `substring` `split` `replace` `replaceAll` `repeat` `search` `normalize` `padStart` `padEnd` `trim` `trimStart` `trimEnd` `toLowerCase` `toUpperCase` `at` `codePointAt` `concat` `toString` `valueOf` | static | static | **Implemented (Task 4.2, second slice).** A closed op table (`STRING_OPS` in `src/hir/nodes.ts`) shared by the gate, the lowering, and the verifier — one vocabulary, three consumers. UTF-16 code-unit semantics throughout; a method is a callee only, never a value. Omitted optional arguments are padded with `undefined` literals at lowering, sound because ECMA-262 treats an explicit `undefined` as absent for every op in the set. `replace`/`replaceAll` implement GetSubstitution (`$$` `$&` `` $` `` `$'`; `$n` stays literal without a RegExp) because Node honors it for plain-string patterns. One honesty clause raises `STA2005` at runtime rather than answer wrongly: `repeat` with a negative or infinite count (spec says RangeError; builtins cannot join the throw protocol yet); `normalize` with a form that is not one of the four raises the same way, for the same reason. **Task 4.3 second slice:** the RegExp-taking forms of `split`/`replace`/`replaceAll` are the SAME op node as their string forms -- the runtime dispatches on the pattern's tag, because a regexp pattern is a scan (repeated `lre_exec`) and a string one is a substring search -- and `search` joined the table, which has no string form at all (the spec builds a RegExp out of its argument, with a constructor this compiler does not have). GetSubstitution grew `$n`/`$nn`, capped at the pattern's group count so an out-of-range number stays literal. **Task 4.3 third slice:** `toUpperCase`/`toLowerCase` above ASCII and `normalize` now go through the vendored libunicode tables (`runtime/src/jsrt_unicode.c`), in code POINTS rather than code units -- one code point can map to three (`ß` -> `SS`), and the Final_Sigma rule makes `Σ` lowercase differently at the end of a word than inside one. **Task 4.4:** `localeCompare`, `toLocaleUpperCase` and `toLocaleLowerCase` joined the table behind the ICU FEATURE BUILD -- collation is a per-locale order and tailored casing a per-locale exception, both of them CLDR data the default runtime does not carry. Without `just runtime-intl` + `STATOR_RUNTIME=intl` the gate refuses all three with `STA1215`, which names the flag rather than a phase; with it, they are accepted only with an EXPLICIT locale, because the absent-locales form reads the HOST's default and would make a compiled program's output depend on the machine that runs it. **Task 4.1 (array-with-properties slice):** `match` joined the table, dispatching to the engine like `search` does. `matchAll` lands as a boxed specialized iterator of match arrays (Phase 5 step 8, plan-notes 151): a `/g` regexp is required (STA2005 otherwise, the spec's TypeError); the matcher is cloned so the original `lastIndex` is untouched; a non-regexp argument stays deferred (RegExpCreate). Deferred members: `split` with a limit, a replacer FUNCTION (it runs user code per match), and the `locales`-as-array / `options` forms of the locale trio, which are Intl negotiation this compiler does not model. Coverage: same dashboard, where the trio's fixtures are `intl_*` and proven by `pnpm run test:intl`. |
| `Array.prototype` (20 methods): `push` `pop` `shift` `unshift` `at` `indexOf` `lastIndexOf` `includes` `join` `slice` `concat` `reverse` `fill` `forEach` `map` `filter` `some` `every` `find` `findIndex` `reduce` `reduceRight` `sort` `flat` `flatMap` `splice` `copyWithin` `findLast` `findLastIndex` `toReversed` `toSorted` `toSpliced` `toString` `with` `keys` `values` `entries` | static | static | **Implemented (Task 4.2, third slice).** The non-callback surface, on the `STRING_OPS` discipline: a closed table (`ARRAY_OPS` in `src/hir/nodes.ts`) shared by gate, lowering, verifier (`STA4082`), and emitter. `includes` searches with SameValueZero and therefore finds `NaN` where `indexOf`'s strict equality cannot — the spec's own asymmetry. `reverse` and `fill` mutate in place and return the receiver; `pop`/`shift`/`at` are typed Unknown (the IndexAccess rule — an empty array really answers `undefined`). Undefined-padding applies except for `lastIndexOf`, whose absent `fromIndex` means `length - 1` while an explicit `undefined` means `0` — it lands without its position argument rather than change an answer. `push`/`unshift` land single-argument (variadic forms have no node to fold into); `concat` takes exactly one array and spreads it. The callback methods (fourth-slice addendum, 2026-08-30) call back into compiled code through `jsrt_call` — the same closure ABI every compiled call site dispatches through, so no new protocol exists: each passes the spec's `(element, index, array)` triple, caches `length` at entry, and coerces predicate answers through `jsrt_truthy` (ToBoolean). `map`/`filter` keep the CHECKER's result type (result kind `mapped`) — map's element is the callback's to choose, and a type-guard `filter` legitimately narrows below the receiver's. The thisArg form is deferred for all of them, and the callback argument must be function-typed (an `any` callback in js mode defers rather than reaching `jsrt_call` unvetted). `reduce`/`reduceRight` land in their WITH-initial form (result kind `checker`: the accumulator type is whatever the callback and initial agreed on, nothing pinned); the zero-initial form is deferred because the first element becomes the seed there and an explicit `undefined` initial IS an initial — the forms cannot share an undefined-padded signature. `sort` is a stable merge (stability is normative; the scratch is a real jsrt array so a collector can see elements mid-merge), and an absent comparator equals an explicit `undefined` one — both spell the ToString default, `[10, 9]` staying `[10, 9]`; SortCompare's undefined rule sinks `undefined` elements to the end before any comparator runs. `flat`'s depth pads safely (explicit `undefined` means the default 1); `flatMap` spreads an array answer one level and appends anything else; `splice` lands in its exact two-argument form (`splice(start)` deletes to the END while a padded `undefined` deleteCount would delete nothing — the `lastIndexOf` rule — and insertion is variadic); `copyWithin`'s whole range pads safely. `keys`/`values`/`entries` land as specialized iterators (Phase 5 step 8, plan-notes 150): a for-of operand inlines and allocates nothing; a stored iterator is a `JSRTIterator` whose `next()` answers `{ value, done }`. Deferred: variadic insertion (`push(...)`/`splice` items) has no node to fold into. |
| `Object.keys` / `Object.values` / `Object.entries` | static (`entries`: dynamic) | static (`entries`: dynamic) | **Implemented (Task 4.2, fourth slice).** One runtime walk over the two object layouts: a fixed shape's keys are its class descriptor's field list (declaration order), a dynamic shape's are its shape chain (insertion order) — both ARE the spec's enumeration order, because identifier keys never trigger the integer-first reorder. `keys` is `string[]`; `values` keeps the checker's element; `entries` produces `[string, T]` pairs the HIR cannot type (no tuple), so its element is honestly Unknown and the verdict says `dynamic`. Arguments outside the two layouts (arrays, Maps, primitives) stay deferred — Node answers differently for each than an object walk would. Coverage: same dashboard. |
| `Object.getOwnPropertyNames` / `Object.hasOwn` / `Object.fromEntries` | static (`fromEntries`: dynamic) | static (`fromEntries`: dynamic) | **Implemented (Task 4.2, eleventh slice).** `getOwnPropertyNames` IS the `keys` walk for every object the subset can build — both layouts hold only string-keyed, enumerable own properties, and the two entry points diverge only when non-enumerable properties become expressible. `hasOwn` asks the shape chain or the class descriptor directly: neither layout has a prototype the subset can reach, so “own” needs no second question; the key must be string-typed (a symbol or number is the ToPropertyKey the object model owns). `fromEntries` is the mirror of the walkers — it takes the ARRAY of pairs and BUILDS a dynamic shape, so its result is Unknown, its verdict is `dynamic`, and a duplicate key resolves the way the shape table already resolves one (last value, first position). The `ObjectStaticCall` node carries an argument LIST from this slice on, arity fixed per method by the gate's `OBJECT_STATICS` table and restated in the verifier. Coverage: same dashboard. |
| `Object.assign` | dynamic | not-yet(STA1214) | **Implemented (Task 4.2, thirteenth slice), two-argument form.** The source's own enumerable string keys are copied onto the target through the same `Object.entries` walk, so the copy's order and `Object.entries(source)` can never disagree; an overwrite keeps the key's original position and only a new key appends; the return value IS the target. The TARGET must be a **growable** shape — one with an optional property or an index signature, whose lookups go through the shape table. A fixed shape's reads compile to slot indices decided at build time, so a key `assign` added could never be read back, and refusing the write is the only sound answer short of deoptimizing every literal into a shape table. That is also why the verdict is `dynamic` in ts mode and why js mode cannot reach it at all: growability comes only from an ANNOTATION, and inference from a literal always produces an all-required shape. The variadic form (three or more arguments) is deferred. Coverage: same dashboard. |
| `Object.freeze` / `Object.isFrozen` | static | static | **Implemented (Phase 5 step 11).** A frozen bit on `JSRTObject` / `JSRTDynObject`; a write throws TypeError through `jsrt_throw` and generated C's pending check after the store. `seal`/`isSealed` still wait on a distinct [[Sealed]] bit. Builtin objects (Date, Promise, Map, …) are not this landing. |
| `Object.create` | error(STA1001) | not-yet(STA1204, Phase 8) | `lib.es5.d.ts` types it `any`, which ts mode bans outright, so the ts-mode answer is the any diagnostic rather than a subset one. Its only in-subset spelling, `Object.create(null)` for a prototype-less map, asks for something every object here already is — neither layout has a reachable prototype. It is `STA1204`'s prototype surface, not a Phase 4 gap (plan-notes 125). |
| `JSON.stringify` | static | static | **Implemented (Task 4.2, fifth slice), single-argument form.** Recursive runtime walk (`jsrt_json_stringify`): scalars, strings with full JSON escaping (short escapes, `\u00XX` controls, lone surrogates as `\udXXX` — well-formed JSON.stringify — and UTF-8 for the rest), arrays, both object layouts via the Object.entries walk, class instances. `-0` spells `"0"`, NaN/Infinity spell `null`. Unserializable values (undefined, functions): skipped as object values, `null` as array elements — and REFUSED by the gate as the top-level argument, where the spec answers `undefined` against a `string` type. Cycles abort loudly at run time (STA2005 pattern). The throw mechanism exists as of Phase 5 step 11; converting this abort is follow-up. The replacer/space forms are deferred. Coverage: same dashboard. |
| `JSON.parse` | dynamic | dynamic | **Implemented (Task 4.2, tenth slice), single-argument form.** Recursive-descent parser over the text's UTF-16 code units (`jsrt_json_parse`): JSON objects become dynamic-shape objects (duplicate keys: last value wins, first position kept), arrays become jsrt arrays, and the leaves are strings (escapes decoded, surrogate pairs preserved), numbers (`strtod` on the ASCII the grammar admitted), `true`/`false`/`null`. The result is typed **Unknown** — the text is data the checker cannot see into — so the verdict is `dynamic` and every use is a boundary the program checks (`typeof` narrowing or an `as` cast, the Task 3.5 machinery). In ts mode this means an ANNOTATED binding: `const v: unknown = JSON.parse(t)`; unannotated, the lib's `any` return is refused as STA1003 at the declaration before the gate speaks. Malformed text, a key containing U+0000, nesting past 512, and a non-string argument reaching the call at run time each abort loudly (STA2005 pattern). The throw mechanism exists as of Phase 5 step 11; converting this abort is follow-up. The reviver form is deferred — it runs user code at every node of the result. |
| Every other global: `String`, `Number`, `Boolean`, `parseInt`, `parseFloat`, `JSON`, `globalThis`, `console` as a value | not-yet(STA1214) | not-yet(STA1214) | These need the global object. **The catch-all now names Phase 5** (2026-09-01, plan-notes 136): Phase 4's builtin surface is complete, and what is left of the global set is `Symbol` and the iterator protocol around it, which is Phase 5 step 8. `globalThis` and `Reflect` are Phase 8's and `Proxy` is a ts-mode `never`, so neither moves the majority a catch-all has to answer for. Deferred at the **gate**, deliberately: the lowering builds bindings only for declarations it lowers, so accepting one produced `STA4035` — an internal error, for legal source (plan-notes 61). `Date` and `RegExp` methods keep their own codes (`STA1210`, `STA1211`). |
| A user binding that shadows a global name | static | static | The rule is "declared nowhere in the module", not "spelled like a global". |

## Modules & imports (ESM only)

| Feature | `ts` mode | `js` mode | Notes |
|---|---|---|---|
| `import` declarations (plain named) | static | static | **Implemented (Task 3.11).** Whole-program v0: files merge into one module in topological order, so an import binds nothing — the name resolves to the exporting file's own top-level binding. Relative specifiers must name their file extension (Node's own ESM rule; extensionless is error STA1113). Bare (package) specifiers are not-yet(STA1214, Phase 7). |
| Renamed/default/namespace imports (`x as y`, `import d from`, `import * as ns`) | not-yet(STA1214, Phase 5) | not-yet(STA1214, Phase 5) | Every aliasing shape is refused: name-based merging cannot honor a rename. `import type` renames are fine — erased, nothing to resolve. |
| `export` declarations (named) | static | static | **Implemented (Task 3.11).** `export` is a no-op marker in the merged program; renamed specifiers (`export { x as y }`) are not-yet(STA1214, Phase 5). |
| `export default <literal>` | static | static | **Implemented (Task 3.11).** Lowers to nothing (nothing can import it without a default import, which is refused). Non-literal default exports are not-yet(STA1214, Phase 5). |
| Re-exports (`export { x } from 'y'`) | not-yet(STA1214, Phase 5) | not-yet(STA1214, Phase 5) | An aliasing shape like renames; refused until real per-module namespaces exist. |
| Cross-file top-level name collisions | not-yet(STA1214, Phase 5) | not-yet(STA1214, Phase 5) | One merged namespace means one owner per name; two files declaring the same top-level name (exported or not) are refused, naming both files. |
| `import()` dynamic import, literal specifier | static | static | **Implemented (Phase 5 step 10).** A module namespace is an `HObject` with `namespace: true`; `ns.x` compiles to the export's global slot (live bindings, not a snapshot). Literal `import("./m.ts")` is `Promise.resolve` of that dummy object, and the specifier is a value-import edge so `m`'s top-level has already run. Printing / `Object.keys` / the spec's Module exotic object are not this landing. Computed specifier stays not-yet (`STA1207`, Phase 8). `import * as ns` is still `STA1214` (step 12). |
| Top-level `await` | static | static if typed, else dynamic | **Implemented (Phase 5 step 9).** The merged module body is an async unit on Task 4.6's resume points: named bindings stay globals, temps live in a heap environment so they survive a suspension, and `main` starts that unit then drains the microtask queue. Init order is Task 3.11's topological order — Stator does not interleave sibling subgraphs the way Node does (docs/MODES.md). |
| Cyclic module imports | error(STA3001), source-located | error(STA3001), source-located | **Implemented (Task 3.11).** Cycles rejected outright; the diagnostic spells the cycle (`a.ts → b.ts → a.ts`). Never a silently-picked order: ESM gives cycles well-defined semantics only via live bindings + TDZ, which a merged namespace cannot express honestly. |

---

## Dynamic escape hatches (errors or not-yet)

| Feature | `ts` mode | `js` mode | Notes |
|---|---|---|---|
| `var` declarations, hoisting | error(STA1104) | static (function-scoped, hoisted, initialized `undefined`) | `var` forbidden in `ts` mode (use `let`/`const`); in `js` mode, lowered to function scope with hoist semantics. |
| `arguments` binding | error(STA1105) | not-yet(STA1202, Phase 5 stretch) | `arguments` forbidden in `ts` mode (use rest parameters); `js` mode support deferred due to lack of fast path for untyped arrays. |
| `eval()` function | error(STA1101), permanent | not-yet(STA1206, Phase 8) | Permanent error in `ts` mode by design (no dynamic code generation). `js` mode defers to Phase 8 (interpreter tier with QuickJS-NG). |
| `new Function()` | error(STA1103), permanent | not-yet(STA1206, Phase 8) | Permanent error in `ts` mode. `js` mode depends on Phase 8 interpreter tier. |
| `Proxy` object | error(STA1106) | not-yet(STA1203, Phase 8) | Proxies intercept all property access via traps; no static analysis of their behavior exists. `ts` mode rejects; `js` mode defers to Phase 8 (interpreter tier). |
| Prototype mutation: `Object.setPrototypeOf()`, `__proto__` writes | error(STA1107) | not-yet(STA1204, Phase 8) | Prototype changes after object construction break compile-time shape assumptions. Forbidden in `ts` mode; deferred in `js` mode. |
| `delete` on class field (instance or static) | error(STA1108) | not-yet(STA1205, Phase 8) | Class fields are compile-time-known struct members; deletion requires runtime shape changes. Forbidden in `ts` mode; deferred in `js` mode. |
| `with` statement, sloppy mode | error(STA1109) | error(STA1109) | ESM is always strict, and `with` is illegal in strict mode — so this is not a Stator restriction, it is the language's. |
| CommonJS `require()` | error(STA1110) | error(STA1110) | ESM is the only module system in both modes. |

---

## Out of scope for v1

| Feature | `ts` mode | `js` mode | Notes |
|---|---|---|---|
| `.tsx`, `.jsx` JSX syntax | error(STA1111) | error(STA1111) | JSX deferred; `.tsx` and `.jsx` files rejected in both modes. React and JSX-using libraries out of scope for MVP. (A `.js` file in `ts` mode is a different diagnostic: STA1002, which points at `--mode=js`.) |
| Decorators **[proposed]** | error(STA1112) | error(STA1112) | Decorators (TC39 stage 3) are a v1 non-goal in both modes. A `never` code, not a `not-yet` one: no phase currently promises them. |

---

## Codes allocated by this document

`docs/DIAGNOSTICS.md` is the authoritative allocator (plan §4 Task 1.3): every code below has a row there with its exact message template. This section records *which* codes the matrix needed and why, not what they say.

### "Never" class (errors by design, STA10xx/STA11xx)

All codes in this range reused from plan.md except the four below.

| Code | Feature | Mode | Meaning |
|---|---|---|---|
| STA1106 | `Proxy` | `ts` | Proxy objects prevent static analysis; forbidden in strict-static mode. |
| STA1107 | Prototype mutation (`Object.setPrototypeOf`, `__proto__` write) | `ts` | Prototype changes after construction break shape assumptions; forbidden in strict-static mode. |
| STA1108 | `delete` on class field | `ts` | Class field deletion requires runtime shape changes; struct layout is compile-time fixed. |
| STA1112 | Decorators | both | A v1 non-goal in both modes; no phase promises them, so this is a `never` code. |

### "Not yet" class (planned, STA12xx)

All codes in this range reused from plan.md except those listed below.

| Code | Feature | Mode | Phase | Meaning |
|---|---|---|---|---|
| STA1201 | Generator methods, async generators, `for await` | both | Phase 5 | `function*` declarations and unnamed expressions landed with step 8 (plan-notes 152). What remains is a receiver in the heap environment (methods), both machines at once (async generators), and the async iterator protocol (`for await`). |
| STA1202 | `arguments` binding | `js` | Phase 5 stretch | Untyped `arguments` arrays lack a fast path; support deferred. |
| STA1203 | `Proxy` | `js` | Phase 8 | Requires interpreter tier (QuickJS-NG) for trap interception. |
| STA1204 | Prototype mutation | `js` | Phase 8 | Interpreter tier allows runtime shape changes. |
| STA1205 | `delete` on class field | `js` | Phase 8 | Interpreter tier allows runtime property deletion. |
| STA1206 | `eval()` **and** `new Function()` | `js` | Phase 8 | One code, because both land with the same interpreter tier and neither can ship without it. |
| STA1207 | `import()` with a computed specifier | both | Phase 8 | Literal-specifier half landed in Phase 5 step 10. Computed specifier needs runtime module resolution the whole-program model lacks. |
| STA1208 | Top-level `await` | both | — | **Landed (Phase 5 step 9, plan-notes 155).** No longer emitted. The module body is an async unit; init is Task 3.11's topological order, not Node's sibling interleaving. The code stays allocated so it is never reused. |
| STA1210 | `Date` residue: the ICU-dependent string forms | both | **none** (feature build) · Phase 5 for arity/spread | Slices A and B both landed 2026-09-01 (plan-notes 132, 133), so what is left under this code is exactly the members whose output needs ICU data: `toString`, `toTimeString` (the zone's long display name) and the three `toLocale*`. Nothing time-zone-dependent remains -- the local family landed under a `TZ=UTC` golden pin plus non-UTC runtime unit tests. `Date.now()` and zero-argument `new Date()` are ACCEPTED under the determinism carve-out, not deferred. The member residue carries **no phase** as of 2026-09-01 (plan-notes 136) -- a feature build is a flag to turn on, not a release to wait for, the `STA1215` model -- while the code's arity and spread refusals are ordinary lowering work under Phase 5. |
| STA1211 | `RegExp.prototype.compile` | both | Phase 8 | Everything else under this code has landed: `test()` with Task 4.3, `exec()`/`match()` with Task 4.1's array-with-properties slice, and the eleven data properties plus `toString` with Task 4.2. `compile` is Annex B §B.2.4 legacy that RE-INITIALIZES an existing RegExp in place, which is the mutate-a-built-object surface Phase 8 owns with `STA1204` -- not a builtin Phase 4 declined to write, which is why it moved off Phase 4 on 2026-09-01 (plan-notes 121, 136). Its variadic arity is a second reason the current op table cannot express it. `String.prototype.matchAll` landed with Phase 5 step 8 (plan-notes 151). |
| STA1212 | `Symbol` primitive type | both | Phase 5 | Well-known symbols as values, Symbol.for/Symbol.keyFor, and registry. `[Symbol.iterator]()` as a class method name is not this code. |
| STA1213 | `BigInt` primitive type | both | Phase 5 | Separate numeric type with dedicated arithmetic. |
| STA1216 | other `Promise.prototype` members; `new Promise` not arity 1 | both | Phase 5 | `then`/`catch`/`finally` and arity-1 `new Promise` landed in step 11. The code stays allocated for the rest. |

`STA1102` appeared in an early draft of this matrix for eval-in-`js`-mode. It is **retired** and must never be reused — it put a "not yet" verdict inside the `STA11xx` "never" range, which is exactly the confusion the two ranges exist to prevent. See the retired-codes table in `docs/DIAGNOSTICS.md`.

---

## Notes and clarifications

- **Decidability:** Every row is testable via `stator explain --json` on a minimal program exercising that feature. Pre-Phase-1-implementation tests carry `// @expected-fail: true` and are reported (never hidden) by the test runner (§4 Task 1.4).
- **Boundary checks:** All "dynamic" verdicts rely on `jsrt_check_*()` functions at type-narrowing points. A runtime type mismatch is a located error, never silent corruption.
- **Specialization:** Features marked "static if typed, else dynamic" route to the static fast path only when the TS compiler can infer a concrete type. Untyped code in `js` mode takes the dynamic path using tagged `jsrt_value`.
- **Phase clock:** "Not yet" features are gated by the phase system in plan.md; out-of-scope features are permanently off v1's roadmap. No feature moves earlier than its phase without a plan amendment (plan §15.3).
- **Decision boundary:** The same feature may differ between modes because `ts` mode rejects untyped code outright (error), while `js` mode lowers it to the dynamic path (tagged values, shape tables, inline caches). This is by design (plan §1).
