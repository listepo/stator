// @mode: js
// @verdict: not-yet
// @code: STA1214
// SUBSET.md: console
// Every console member has a fixed arity with a documented optional tail. `groupEnd` takes none,
// so an argument is a form the gate does not admit -- not a silently ignored extra.

console.groupEnd(1);
